name: GitHub Release

on:
  push:
    tags: ['v*.*.*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node scripts/github-release.mjs --backfill
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}